// 两个参数count， limit， count总页数， limit每一页的数字按钮个数
// 当前页数current
// 根据以上三个初始化

class Pager {
  constructor(options){
    this.defaultOptions = {
      count: 10,
      currentPage: 1,
      limit: 10
    }
  }

}